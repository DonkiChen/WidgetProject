<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.widgetproject.main">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="test.widgetproject.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="test.widgetproject.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LinkRecyclerViewActivity" />
        <activity android:name=".SwipeImageVerifyActivity" />
        <activity android:name=".CityActivity" />
        <activity android:name=".LabelActivity" />
        <activity android:name=".RevealActivity" />
        <activity android:name=".RoundCornerActivity" />
        <activity android:name=".PatternLockActivity" />
        <activity android:name=".TopicActivity" />
        <activity android:name=".RulerActivity" />
        <activity android:name=".LazyActivity" />
        <activity android:name=".ShareActivity" />
        <activity android:name=".SharePreviewActivity" />
    </application>

</manifest>